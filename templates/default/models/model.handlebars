{{#if modelReferences.length}}
import {
{{#modelReferences}}
  {{this}}{{#unless @last}},{{/unless}}
{{/modelReferences}}
} from './index'

{{/if}}export interface {{ className }}{{#if extends.length}} extends {{#extends}}{{this}}{{#unless @last}}, {{/unless}}{{/extends}}{{/if}} {
{{#properties}}
  {{name}}: {{type}};
{{/properties}}
}